import React, { Component } from 'react';
import { render } from 'react-dom';
import './style.css';

/*
   Your challenge should you choose to accept it:
   1. Fetch a list of users from https://jsonplaceholder.typicode.com/users.
   2. Display the users ID, Name and Email, as a table, where each raw represent a single user
   3. Once a raw was clicked, the user's name should be striked and pained in red.

   Bonus: 
   It should be possible to reorder the users, by dragging and dropping a user to the appropriate position.
   */

const App = () => {
  return <div>{/* Implement Me !!! */}</div>;
};

render(<App />, document.getElementById('root'));
